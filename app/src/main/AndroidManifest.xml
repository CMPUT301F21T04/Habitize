<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.habitize" >

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Habitize">
        <activity
            android:name=".CustomListOfSearchResults"
            android:exported="false" />
        <activity
            android:name=".SearchResults"
            android:exported="false" />


        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:exported="false" />
        <activity
            android:name=".AddHabitTabsBase"
            android:exported="true" />
        <activity
            android:name=".CustomListOfExistingFollowers"
            android:exported="false" />
        <activity android:name=".FollowRequests"/>
        <activity android:name=".ViewRecordBase"
            android:parentActivityName=".ViewHabitTabsBase"/>
        <activity
            android:name=".FollowingActivity"
            android:exported="false" />
        <activity android:name=".ViewHabitTabsBase"
            android:parentActivityName=".AllHabitsActivity"/>
        <activity
            android:name=".Login_Activity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AllHabitsActivity" />
        <activity android:name=".CreateRecordBase"
            android:exported="true"
            android:parentActivityName=".AllHabitsActivity"/>
        <activity
            android:name=".TodaysHabitsActivity"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>